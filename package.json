{
    "name": "explainable-ai-dashboard",
    "version": "1.0.0",
    "private": true,
    "scripts": {
      "start": "cd frontend && npm start",
      "build": "cd frontend && NODE_OPTIONS='--openssl-legacy-provider' npm install && npm run build"
    },
    "dependencies": {
      "axios": "^0.21.1"
    }
  }